import { HtmlToColorDirective } from './html-to-color.directive';

describe('HtmlToColorDirective', () => {
  it('should create an instance', () => {
    const directive = new HtmlToColorDirective();
    expect(directive).toBeTruthy();
  });
});
